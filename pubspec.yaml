name: mela_sento
description: A high-fidelity social betting app.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  glass_kit: ^4.0.2
  fl_chart: ^0.66.0
  animate_do: ^3.0.2
  supabase_flutter: ^2.12.0
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
